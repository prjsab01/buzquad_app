# Coding Rules

- Use React + TypeScript with strong typing.
- Preserve project architecture and incremental changes.
- Avoid unnecessary refactors.
- Document progress in `.agent` files.
- Keep external dependencies minimal and free-tier compatible.
